#!/bin/bash

DIR=$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )/..

"$DIR/bin/publish" || exit 1
echo "--------------------------------------------------------------------------------"
echo "prevent publishing $DIR"
echo "--------------------------------------------------------------------------------"
exit 1