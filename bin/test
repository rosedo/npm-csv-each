#!/bin/bash

DIR=$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )/..

if test -f "$DIR/node_modules/.bin/mocha"; then
  "$DIR/node_modules/.bin/mocha" "$DIR/test" || exit 1
else
  mocha "$DIR/test" || exit 1
fi